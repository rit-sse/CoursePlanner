dependencies:
    pre:
        - npm install
        - bower install
        - npm install -g gulp
        - gulp

test:
    pre: 
        - npm start:
            background: true
        - sleep 5
    override:
        - npm run karma
        - npm run protractor
